import {StyleSheet} from 'react-native';
import {centerContent} from '../../utils/globalStyle';

export default StyleSheet.create({
  container: {
    ...centerContent,
    justifyContent: 'center',
  },
  imageContainer: {},
});
